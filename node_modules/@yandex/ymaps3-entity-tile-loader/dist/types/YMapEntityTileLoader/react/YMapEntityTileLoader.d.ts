import type TReact from 'react';
import type { YMapEntity } from '@yandex/ymaps3-types';
import type { CustomReactify, OverrideProps } from '@yandex/ymaps3-types/reactify/reactify';
import type { GeojsonFeature, YMapEntityTileLoader as YMapEntityTileLoaderI, YMapEntityTileLoaderProps } from '../YMapEntityTileLoader';
/**
 * Create reactified version of YMapEntityTileLoader module
 *
 * @example
 * ```jsx
 * <YMap location={LOCATION} ref={x => map = x}>
 *     <YMapDefaultSchemeLayer />
 *     <YMapDefaultFeaturesLayer />
 *     <YMapEntityTileLoader
 *          tileSize={TILE_SIZE}
 *          getFeatureId={useCallback((feature) => feature.id, [])}
 *          fetchTile={fetchTile}
 *          entity={useCallback(
 *              (feature) => (
 *                  <YMapFeature geometry={feature.geometry} properties={feature.properties} />
 *              ),
 *              []
 *          )}
 *          onFeatureAdd={useCallback((entity) => {
 *               setTotal((total) => total + entity.properties.area_sqkm);
 *          }, [])}
 *          onFeatureRemove={useCallback((entity) => {
 *              setTotal((total) => total - entity.properties.area_sqkm);
 *          }, [])}
 *     />
 * </YMap>
 * ```
 */
type YMapEntityTileLoaderReactifiedProps = OverrideProps<YMapEntityTileLoaderProps, {
    /** Function that returns YMapEntity react component to render feature*/
    entity: (feature: GeojsonFeature) => TReact.ReactElement;
    /**
     * Every time a new tile is loaded and new features are added,
     * React will re-calls render for each added feature.
     * To avoid this, you can set renderDelay (in milliseconds).
     * After this time, React will render all features at once render call.
     * @default 300
     */
    renderDelay?: number;
}>;
type YMapEntityTileLoaderR = TReact.ForwardRefExoticComponent<YMapEntityTileLoaderReactifiedProps & TReact.RefAttributes<YMapEntity<YMapEntityTileLoaderReactifiedProps>>>;
export declare const DEFAULT_THROTTLE_TIMOUT = 300;
export declare const YMapEntityTileLoaderReactifyOverride: CustomReactify<YMapEntityTileLoaderI, YMapEntityTileLoaderR>;
export {};
