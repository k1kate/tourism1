import { PixelCoordinates } from "@yandex/ymaps3-types";
export declare const computeTileBounds: (pixelCenter: PixelCoordinates, viewportSize: PixelCoordinates, scale: number, tileSize: number) => {
    minX: number;
    minY: number;
    maxX: number;
    maxY: number;
};
